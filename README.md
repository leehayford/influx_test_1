# influx_test_1


Frontend ( Terminal 1 )
```console
cd client
sudo npm install
sudo npm run build
```

Backend ( Terminal 2 )
```console
env\scripts\activate
sudo pip install -r req.txt
uvicorn main:app --reload
```



